const fs = require('fs'); const lines = fs.readFileSync('src/main/resources/templates/fragments/sjuit_components.html', 'utf8').split('\n'); let divCount = 0; let divStarts = []; for (let i = 0; i < lines.length; i++) { const starts = (lines[i].match(/<div(?=[\s>])/g) || []).length; const ends = (lines[i].match(/<\/div>/g) || []).length; for (let j = 0; j < starts; j++) { divStarts.push(i + 1); } for (let j = 0; j < ends; j++) { divStarts.pop(); } } console.log('Unclosed divs started on lines:', divStarts);