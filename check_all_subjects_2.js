const { MongoClient } = require('mongodb'); async function run() { const client = new MongoClient('mongodb://localhost:27017'); await client.connect(); const db = client.db('school_db'); const subjects = await db.collection('subjects').find({ name: /SERVER ADMINISTRATION/i }).toArray(); console.log(subjects.map(s => ({name: s.name, level: s.levelNo, sem: s.semesterNo}))); await client.close(); } run().catch(console.dir);