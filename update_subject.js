const { MongoClient } = require('mongodb'); async function run() { const client = new MongoClient('mongodb://localhost:27017'); await client.connect(); const db = client.db('school_db'); await db.collection('subjects').updateMany({ name: 'MICROPROCESSOR AND MICROCONTROLLER' }, { $set: { name: 'MICROCONTROLLER AND MICROPROCESSOR' }}); console.log('Updated subject in DB'); await client.close(); } run().catch(console.dir);