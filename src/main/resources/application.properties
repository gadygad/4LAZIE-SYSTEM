<<<<<<< HEAD
# =====================
# SERVER
# =====================
server.port=${PORT:8082}

# =====================
# DATABASE (RENDER SAFE)
# =====================
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
=======
# Server Configuration
server.port=${PORT:8080}

# Database Configuration (H2 File-based for Persistence)
spring.datasource.url=jdbc:h2:file:./school_db;DB_CLOSE_DELAY=-1;MODE=MySQL;AUTO_SERVER=TRUE
>>>>>>> cb17350 (Clean Railway deployment: remove build artifacts, database files, and prepare production build)
spring.datasource.username=sa
spring.datasource.password=

# =====================
# JPA / HIBERNATE
# =====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# =====================
# THYMELEAF
# =====================
spring.thymeleaf.cache=false

# =====================
# H2 CONSOLE (OPTIONAL)
# =====================
spring.h2.console.enabled=true
