/**
 * Data Transfer Object (DTO) layer.
 *
 * <p>DTOs carry data between the controller and service layers,
 * decoupling the API contract from the internal domain model.
 *
 * @since 1.0
 */
package com.school.dto;
