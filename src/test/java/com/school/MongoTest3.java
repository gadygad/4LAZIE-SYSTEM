package com.school; import org.junit.jupiter.api.Test; import org.springframework.beans.factory.annotation.Autowired; import org.springframework.boot.test.context.SpringBootTest; import com.school.repository.NoteRepository; import com.school.model.Note; import java.util.List; @SpringBootTest public class MongoTest3 { @Autowired private NoteRepository noteRepository; @Test public void test() { List<Note> notes = noteRepository.findAll(); for(Note n : notes) { if(n.getLevelNo() != null && n.getLevelNo() == 5 && n.getSemesterNo() != null && n.getSemesterNo() == 2) { System.out.println("NOTE TITLE: " + n.getTitle() + " UNIT: " + n.getUnitNumber()); } } } }